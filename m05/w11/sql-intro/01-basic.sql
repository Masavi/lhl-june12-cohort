-- SELECT statement allows to choose which columns
-- FROM allows to choose which table we are fetching data from

SELECT id, title FROM books;
SELECT title FROM books;
SELECT * FROM books;

SELECT * FROM authors;
SELECT name FROM authors;
